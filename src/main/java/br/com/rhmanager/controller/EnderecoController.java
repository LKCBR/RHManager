package br.com.rhmanager.controller;

import br.com.rhmanager.util.CEPWebService;
import javafx.scene.control.ComboBox;
import javafx.scene.control.TextField;

/**
 *
 * @author lucas
 */
public class EnderecoController {

    /* public void getEndByCEP(String cep, TextField tfRua, TextField tfBairro, TextField tfCidade, ComboBox cbEstados) {
        String valor = cep.replaceAll(" ", "");

        if (valor.length() == 10) {

            CEPWebService.getEnderecoByCEP(valor, tfRua, tfBairro, tfCidade, cbEstados);

        }
    }*/
}
